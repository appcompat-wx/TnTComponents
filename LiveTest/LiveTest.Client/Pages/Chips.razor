@page "/chips"
@using TnTComponents
@rendermode InteractiveAuto


<TnTComponents.TnTChip Label="Chip" @bind-Value="_a" BindAfter="Checked" />
<TnTComponents.TnTChip StartIcon="@(new MaterialIcon{ Icon = MaterialIcon.GridOn })" Label="Chip" BindAfter="Checked" @bind-Value="_b" />
<TnTComponents.TnTChip Label="Chip" CloseButtonClicked="@Closed" @bind-Value="_c" BindAfter="Checked" />
<TnTComponents.TnTChip StartIcon="@(new MaterialIcon{ Icon = MaterialIcon.GridOn })" Label="Chip" CloseButtonClicked="@Closed" BindAfter="Checked" @bind-Value="_d" />


@code {
    private bool _a;
    private bool _b;
    private bool _c;
    private bool _d;


    private void Checked(bool b) {
        Console.WriteLine(b);
    }

    private void Closed() {
        Console.WriteLine("Closed");
    }
}

